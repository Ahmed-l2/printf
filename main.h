#ifndef MAIN_H
#define MAIN_H

#include <limits.h>
#include <stdio.h>
#include <stdlib.h>
#include <stdarg.h>
#include <unistd.h>


int intToStr(int num);
int _puts(char *str);
int _putchar(char c);
int _printf(const char *format, ...);

#endif
